# DSH-ClipClop

DSH-ClipClop 是 DeepSeek Harness 插件集合。第一个插件 dsh-navigator 用于在长任务执行期间定期复核任务航向。

## 术语

**主会话**：执行用户任务的对话历史。避免使用“主任务”“主 Session”“主线程”“父上下文”。

**自主执行区间**：从最近一次真实用户消息开始，到下一次真实用户消息到达或执行被取消为止的主会话执行区间。助手更新、工具结果和插件消息不算用户介入。避免使用“无人值守任务”“自主轮次”。

**步骤**：一次模型请求及其产生的工具调用。一个 turn 可以包含多个步骤。避免使用“轮次”“工具调用”代替此术语。

**航向复核**：在某个触发点，使用主会话上下文评估目标、进展、阻塞、成本和当前方案。它不是跑偏检测保证，也不是普通提醒。

**复核上下文快照**：触发点上主会话中模型可见的对话内容，用作航向复核的证据。它不是 Session fork，也不是独立判断者上下文。

**等待模式**：主会话在开始下一步前等待航向复核完成的执行模式。

**并行模式**：主会话继续执行，同时复核较早上下文快照的执行模式。

**触发点**：航向复核开始时主会话历史所在的位置。

**复核结论**：航向复核对主会话后续行为的结论，包括 continue、adjust 和 stop。每条结论都包含原因与建议；stop 的原因必须能向用户说明。

**复核建议**：`adjust` 与并行模式 `stop` 结论的内容，作为干预上下文进入主会话。它是建议而非指令，复核不代为执行回滚。

**干预上下文**：被加入主会话、用于影响后续任务执行的复核内容。continue 默认不产生干预上下文。
