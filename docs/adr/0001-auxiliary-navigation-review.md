# 使用辅助模型请求执行航向复核

状态：已接受。

dsh-navigator 使用继承主任务模型配置的辅助模型请求，评估主任务当前模型可见上下文，不创建子 Session 或运行时 Agent。现有 Harness Session fork 只接受不在活动 turn 内的历史，无法直接覆盖长任务最近完成的步骤；辅助请求能够提供这些证据，并允许按结论选择性写入主会话。代价是复核没有可浏览的子 Session，也不能执行工具；复核记录保留在主模型上下文之外供诊断使用。
